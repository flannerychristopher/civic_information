import React from 'react';

const CurrentVotes = ({ currentVotes }) => {
  console.log('rendering current votes: ', currentVotes);
  return (
    <div>
      current CurrentVotes

    </div>
  )
}

export default CurrentVotes;